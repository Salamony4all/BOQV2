# HANDOFF — BOQFLOW

## 🚀 Recent Accomplishments (Apr 14, 2026)
- **Standardized Default Branding**: Established **Alshaya Enterprises** as the bedrock default configuration.
- **Branding Assets**: Migrated high-resolution logos (`1561x865px`) into `src/assets/` and updated `CompanyContext.jsx` to reflect these exact dimensions.
- **Dynamic Document Generation**: Removed hardcoded URLs from `TableViewer.jsx`. The application now dynamically pulls `website`, `logo`, and `companyName` from the context for all PDF/PPT exports, while defaulting to Alshaya.
- **Context Hardening**: Updated `CompanyContext` to initialize with `setupComplete: true` for the Alshaya profile, ensuring a smooth first-load experience without forced setup modals, while maintaining the ability to edit via the hamburger menu.
- **Repo Hygiene**: Optimized `.gitignore` to exclude `scratch/`, `logos/` (source), and temporary text logs.

## 🛠 Project Structure Recap
- **Frontend**: React + CSS Modules. Contexts in `/src/context` are critical (`CompanyContext`, `ScrapingContext`).
- **Backend**: Express app in `/server`. AI pipeline (Search -> Local -> Discover) is the "magic" sauce.
- **Data**: Brand catalogs are JSON files in `/server/data/brands`.

## ⏭ Immediate Next Steps for Next Agent
1. **AI Match Category Refinement**: Address the "Meeting Table vs Coffee Table" labeling issue in the NARBUTAS and Las catalogs. AI needs to better distinguish based on size context (e.g., `R:30` vs `120x80`).
2. **Single-Row AI Retry**: Currently, the "Retry" button in `MultiBudgetModal` only clears status. Implement a single-row AI trigger to avoid re-running the whole batch.
3. **PM Export Expansion**: The user specifically mentioned **MAS, MIR, WIR** exports. These should be specialized formats in `TableViewer.jsx` or separate components.
4. **Enhanced Scraping Fallbacks**: Improve Stage 3 discovery for brands with heavy JavaScript (using the Playwright-based `js-scraper-service` more effectively).

## 💡 Pro-Tips
- **Hamburger Button**: The main way to change company branding is the hamburger icon in the header.
- **Image Proxying**: Always use `/api/image-proxy?url=` when displaying external brand images to avoid CORS and security issues.
- **API Base**: Use `getApiBase()` to ensure local/prod connectivity works out of the box.

---
*Generated by Antigravity on Apr 14, 2026*
